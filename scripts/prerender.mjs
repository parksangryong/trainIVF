import { mkdir, readFile, rm, writeFile } from "node:fs/promises";
import { dirname, join } from "node:path";
import { fileURLToPath } from "node:url";
const root=join(dirname(fileURLToPath(import.meta.url)),".."); const template=await readFile(join(root,"dist/index.html"),"utf8"); const {render,getSeo,getStaticRoutes}=await import(join(root,"dist-server/entry-server.js")); const routes=process.argv.slice(2).length?process.argv.slice(2):getStaticRoutes();
for(const route of routes){const seo=getSeo(route);const html=template.replace(/<div id="root">[\s\S]*?<\/div>/,`<div id="root">${render(route)}</div>`).replace(/<title>[\s\S]*?<\/title>/,`<title>${seo.title}</title>`).replace(/<meta name="description" content="[\s\S]*?"\s*\/>/,`<meta name="description" content="${seo.description}" />`).replace(/<meta property="og:title" content="[\s\S]*?"\s*\/>/,`<meta property="og:title" content="${seo.title}" />`).replace(/<meta property="og:description" content="[\s\S]*?"\s*\/>/,`<meta property="og:description" content="${seo.description}" />`).replace(/<meta name="twitter:title" content="[\s\S]*?"\s*\/>/,`<meta name="twitter:title" content="${seo.title}" />`).replace(/<meta name="twitter:description" content="[\s\S]*?"\s*\/>/,`<meta name="twitter:description" content="${seo.description}" />`).replace(/<link rel="canonical" href="[\s\S]*?"\s*\/>/,`<link rel="canonical" href="${seo.canonical}" />`);const output=route==="/"?join(root,"dist/index.html"):join(root,"dist",route.slice(1),"index.html");await mkdir(dirname(output),{recursive:true});await writeFile(output,html)}
const sitemap=`<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n${routes.map((route)=>`  <url><loc>https://trainivf.com${route}</loc><changefreq>monthly</changefreq></url>`).join("\n")}\n</urlset>\n`;await writeFile(join(root,"dist/sitemap.xml"),sitemap);await rm(join(root,"dist-server"),{recursive:true,force:true});
